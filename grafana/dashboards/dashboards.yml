apiVersion: 1
providers:
  - name: 'Railway'
    orgId: 1
    folder: 'Railway'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards-json
